# Check out Heroku's Pipeline documentation on when this is run.
# https://devcenter.heroku.com/articles/release-phase#when-does-the-release-command-run
# The build command can be replaced with a .sh file if multiple things need to get done.
release: JEKYLL_ENV=production bundle exec jekyll build
